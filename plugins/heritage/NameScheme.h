
//NameScheme: how to choose the last names of dwarves

//note the intentional lack of #pragma once!
X(normal)
    //get two of your parents' names, depending on age
X(eldestParent)
    //get name of eldest parent
X(fatherName)
    //get name of father
X(motherName)
    //get name of mother

