


/*
 * OutputType: Describes the policy of what should be printed while the plugin executes.
 **/

X(none) //do not output anything
X(progress) //only output progress of computing new names
X(fortAliveNames) //print statistics on names of living dwarves in your fort
X(fortNames) //print stats on names of dwarves in your fort
X(aliveNames) //print stats on names of living dwarves in the world
X(allNames) //print stats on any names any dwarf has had in the world

